<?xml version="1.0" encoding="utf-8"?>

<searchable xmlns:android="http://schemas.android.com/apk/res/android"
    android:label="@string/app_name"
    android:hint="@string/search_hint"
    android:imeOptions="actionSearch"
    android:searchSettingsDescription="@string/search_settings"

    android:voiceSearchMode="showVoiceSearchButton|launchRecognizer"
    android:voiceLanguageModel="web_search"
    android:voicePromptText="Diga algo amigo"

    android:searchSuggestAuthority="io.github.filipebezerra.tastingsearchview.provider.SuggestionsProvider"
    android:searchSuggestIntentAction="android.intent.action.VIEW"
    android:searchSuggestSelection="query=?"
    android:searchSuggestThreshold="2"/>